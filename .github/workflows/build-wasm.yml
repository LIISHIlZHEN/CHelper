name: build-wasm

on:
  push:
    branches: [ "master", "dev" ]
    paths:
      - '.github/workflows/build-wasm.yml'
      - 'CHelper-Core/**'
      - 'scripts/**'
  pull_request:
    branches: [ "master", "dev" ]
    paths:
      - '.github/workflows/build-wasm.yml'
      - 'CHelper-Core/**'
      - 'scripts/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - name: Install dependencies
        run: sudo apt-get install -y cmake ninja-build

      - name: Build Web Core
        run: python ./scripts/build_web_core.py
